package com.demowebshop.listeners;

import org.testng.ITestListener;

import com.demowebshop.base.BaseClass;

public class MyListeners  implements ITestListener {
	
/*
	Logger logger = LogManager.getLogger(this.getClass());

	@Override
	public void onStart(ITestContext context) {// Executes once before all the test method

		logger.info("========== Test Suite Started : {} ==========", context.getName());
	}

	@Override
	public void onTestStart(ITestResult result) {//For Every Test 

		logger.info("Test Started : {}", result.getMethod().getMethodName());

	}

	@Override
	public void onTestSuccess(ITestResult result) {

		logger.info("Test Passed : {}", result.getMethod().getMethodName());

	}

	@Override
	public void onTestFailure(ITestResult result) {

		logger.error("Test Failed : {}", result.getMethod().getMethodName());

		logger.error("Reason : {}", result.getThrowable());

		// Capture Screenshot
		// Attach Screenshot to Extent Report

	}

	@Override
	public void onTestSkipped(ITestResult result) {

		logger.warn("Test Skipped : {}", result.getMethod().getMethodName());

	}

	@Override
	public void onFinish(ITestContext context) {

		logger.info("========== Test Suite Finished : {} ==========", context.getName());

	}
*/

}








